services:
  mongodb:
    image: mongo:latest
    container_name: quizflow_db
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:alpine
    container_name: quizflow_redis
    ports:
      - "6379:6379"

volumes:
  mongo_data:
